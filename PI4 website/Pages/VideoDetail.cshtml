@page "{id?}"
@using PI4_website.Models;
@model PI4_website.Pages.VideoDetailModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
}


@* <form method ="post">
Voeg onderwerp toe aan video: <input asp-for="Videos.Name"/>
<input asp-for="Videos.VideoID" type="hidden" />
    <input type ="submit" class="btn btn-primary"/>
</form> *@

@* //asp-items="@Model.DropdownList"
 *@
<form method="post">
    <h3> Voeg onderwerp toe aan video:</h3>  
@*     <select data-val="true" style="align-self" asp-for="OnderwerpenCollectie" name="OnderwerpID" id="OnderwerpID">
      <option disabled="">Geen keuze mogelijk</option>
        @foreach (var ow in Model.OnderwerpenCollectie)
        {
          <option value="@ow.OnderwerpID">@ow.Name </option>  

        }

        </select> *@

    <select asp-for="OnderwerpID" id=" OndId" name="OndId" asp-items="@Model.OnderwerpList">
        <option>Kies een onderwerp</option>
    </select>

    <div>
        <input type="submit" asp-page-handler="OnderwerpToevoegen" class="btn btn-primary" />
    </div>

   

</form>

<p>
<table class="table">
    


            <thead> Onderwerpen bij deze video:</thead>

        <tr>
@*         <td>@Model.Videos.Name </td> *@
        </tr>
           
        @foreach (var a in Model.Videos.Koppelingen)
       
                {
            <tr>
                 <td>   @a.Onderwerpen.Name</td>

            </tr>

                }
        <td>
            <iframe width="420" height="315"
                    src="@Model.Videos.Link">
            </iframe>
        </td>
      
            


   
    
</table>
</p>